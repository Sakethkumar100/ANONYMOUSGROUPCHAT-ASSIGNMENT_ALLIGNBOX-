const express = require("express")
const router = express.Router()

// This file can be used for additional message-related routes
// Currently, message routes are handled in groups.js for simplicity

module.exports = router
